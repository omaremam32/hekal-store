# Hekal code patch

Copy these files into your project and replace the existing files:

- lib/types.ts
- components/ProductCard.tsx
- components/products/ProductDetail.tsx
- app/layout.tsx

Then run:

npm run dev
npm run build

git add .
git commit -m "Show Hekal factory and Colvert labels"
git push
